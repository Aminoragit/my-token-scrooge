# Security Policy

Report vulnerabilities privately through the repository security advisory form. Do not include secrets, private source, or exploit data in public issues.

Supported releases receive fixes on the latest stable minor version. MTS evaluates policy locally, never uploads telemetry by default, and treats harness input, policy files, paths, shell text, and raw artifacts as untrusted.

