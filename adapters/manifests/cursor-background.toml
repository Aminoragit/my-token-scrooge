# Generated from crates/mts-harnesses/src/lib.rs by adapters/generate.mjs.
schema_version = 1
id = "cursor-background"
display_name = "Cursor Background Agent"
vendor = "unknown"
execution_surface = "Cursor Background Agent"
families = ["REPO_BOOTSTRAP"]
commands = []
markers = ["cursor-background"]
installation_form = "Repository bootstrap"
policy_dir = "~/.mts/harnesses/cursor-background"
minimum_mts_version = "0.1.0"
owner = "MTS adapter maintainers"
source = "crates/mts-harnesses/src/lib.rs"
default_mode = "SHADOW"
capability_grade = "UNVERIFIED"
verification_status = "UNVERIFIED"
planned_capability = "ADVISORY"
install_dry_run = true
install = true
uninstall = true
deployment = "COPY"
owned_files = ["block-full.txt", "block-partial.txt", "adapter.json", "install-manifest.json"]
fixture_dir = "fixtures/contracts/cursor-background/current"
doctor_template = "Detection: {detection}\nVersion: {version}\nIntegration: {integration}\nPolicy files: {policy_files}\nRecommended mode: SHADOW"

[probe]
version_args = ["--version"]
help_args = ["--help"]
contract = "fixtures/contracts/cursor-background/current"

[capabilities]
read = "UNVERIFIED"
write = "UNVERIFIED"
edit = "UNVERIFIED"
shell = "UNVERIFIED"
rewrite_input = false
replace_output = false
remote_executor = false

[honesty]
live_verified = false
unknown_version_mode = "SHADOW"
mcp_is_enforcement_boundary = false
unknown_internal_tools = true
policy_files_are_physical_copies = true
